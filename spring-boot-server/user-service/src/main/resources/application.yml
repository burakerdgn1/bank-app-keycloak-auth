server:
  port: 8081  # Choose a port for the user service

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/userdb
    username: burakerdogan
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
